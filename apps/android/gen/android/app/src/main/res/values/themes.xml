<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.linplayer_android" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!--
          ★ 窗口背景必须**透明**,否则视频永远出不来。

          视频是一层 SurfaceView(见 MainActivity.onWebViewCreate),它排在 WebView
          底下,画面从窗口**下面**透上来。这条链上有三层,任何一层不透明都是
          「有声音没画面」,而且**一律不报错**:
            1. WebView           —— MainActivity 里 setBackgroundColor(TRANSPARENT)
                                    + tauri.conf.json 的 "transparent": true
            2. 前端渲染链         —— tv.css 里 html.playing 那三条
            3. **Activity 窗口**  —— 就是这里

          第 3 层原来是漏的:父主题是 DayNight,windowBackground 跟着系统深浅色走,
          于是**浅色模式一片白、深色模式一片黑** —— 用户 2026-07-21 报的正是这一句。
          它盖在 SurfaceView 上面,声音照放,画面一帧都透不出来。

          ★ 改动这三层中的任何一层,都要回真机确认画面还在:这类故障没有任何日志,
            只能靠眼睛。播放页现在会把 mpv 侧的自检结果显示出来(见 VideoDiag),
            但那只覆盖"mpv 自己没出画面",盖不住"出了画面但被上面某层挡住"。
        -->
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
</resources>
