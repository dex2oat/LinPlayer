import 'package:flutter/widgets.dart';

const BorderRadius desktopLandscapeCoverRadius = BorderRadius.all(
  Radius.circular(10),
);

const BorderRadius desktopPortraitCoverRadius = BorderRadius.all(
  Radius.circular(8),
);
